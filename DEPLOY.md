# GitHub Pages 部署指南

## 🚀 自动部署（推荐）

项目已经配置了 GitHub Actions 自动部署，每次推送到 `main` 分支时会自动部署到 GitHub Pages。

### 启用步骤：

1. **进入 GitHub 仓库设置**
   - 访问你的仓库：https://github.com/huang1074867514/-
   - 点击 `Settings` 选项卡

2. **启用 GitHub Pages**
   - 在左侧菜单中找到 `Pages`
   - 在 `Build and deployment` 下，选择 `Source` 为 `GitHub Actions`

3. **推送代码触发部署**
   ```bash
   git add .
   git commit -m "配置 GitHub Pages 自动部署"
   git push origin main
   ```

4. **查看部署状态**
   - 进入仓库的 `Actions` 选项卡查看部署进度
   - 部署完成后，访问：https://huang1074867514.github.io/-/

## 🛠️ 手动部署

如果需要手动部署，可以使用以下命令：

```bash
# 安装 gh-pages 工具
npm install -g gh-pages

# 构建并部署
npm run deploy
```

## 📁 项目结构

```
.
├── .github/workflows/deploy.yml  # GitHub Actions 配置
├── dist/                         # 构建输出目录
├── vite.config.ts               # Vite 配置（包含 GitHub Pages 路径）
└── package.json                 # 包含部署脚本
```

## ⚙️ 重要配置

### Vite 配置 (vite.config.ts)
```typescript
base: mode === 'production' ? '/vgbp/' : '/',
```
- 开发环境使用根路径 `/`
- 生产环境使用 GitHub Pages 路径 `/vgbp/`

### GitHub Actions 配置
- 自动在推送到 `main` 分支时触发
- 使用 Node.js 18 进行构建
- 自动部署到 GitHub Pages

## 🔧 故障排除

### 1. 部署后页面空白
- 检查浏览器控制台是否有 404 错误
- 确保 `vite.config.ts` 中的 `base` 路径配置正确

### 2. 构建失败
- 检查 `package.json` 中的依赖是否正确
- 确保 `npm run build` 在本地可以正常运行

### 3. 部署失败
- 检查 GitHub Actions 的权限设置
- 确保仓库启用了 GitHub Pages 功能

## 📱 访问地址

部署成功后，你的应用将在以下地址可用：

**主要地址**: https://huang1074867514.github.io/vgbp/

## 🎯 功能特性

部署后的应用包含：
- ✅ 英雄选择界面
- ✅ 中英文搜索功能
- ✅ 响应式设计
- ✅ 深色主题
- ✅ 进度条显示
- ✅ Reset 功能
- ❌ AI 功能（已移除，避免依赖 API 密钥）